<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/black</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="PictureProcessingTabImageView">
        <item name="android:paddingBottom">@dimen/picture_process_tab_padding</item>
        <item name="android:paddingTop">@dimen/picture_process_tab_padding</item>
        <item name="android:layout_weight">1.0</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="PictureProcessingTransformTabMenuImageView" parent="PictureProcessingTabImageView">
        <item name="android:paddingBottom">25dp</item>
        <item name="android:paddingTop">25dp</item>
        <item name="android:background">@color/lessGray</item>
    </style>

    <style name="PictureProcessingTransformTabMenuTextView" >
        <item name="android:textSize">8dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:paddingTop">30dp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="PictureProcessingParamTabMenuTextView" parent="PictureProcessingTransformTabMenuTextView">
    </style>

    <style name="PictureProcessingParamTabMenuImageView" parent="PictureProcessingTransformTabMenuImageView">
        <item name="android:paddingBottom">25dp</item>
        <item name="android:paddingTop">25dp</item>
    </style>

</resources>
